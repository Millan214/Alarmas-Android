<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/btnAlarma"
        android:layout_width="550dp"
        android:layout_height="53dp"
        android:layout_marginStart="20dp"
        android:layout_marginLeft="20dp"
        android:layout_marginTop="120dp"
        android:layout_marginEnd="20dp"
        android:layout_marginRight="20dp"
        android:width="300dp"
        android:background="@drawable/btn_background"
        android:clickable="true"
        android:text="añadir alarma"
        android:textColor="#FFFFFF"
        android:textSize="16sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/btnMostrar"
        android:layout_width="550dp"
        android:layout_height="60dp"
        android:layout_marginTop="12dp"
        android:width="300dp"
        android:background="@drawable/boton_mostrar"
        android:clickable="true"
        android:text="mostrar alarmas"
        android:textColor="#FA71CD"
        android:textSize="16sp"
        app:layout_constraintStart_toStartOf="@+id/btnAlarma"
        app:layout_constraintTop_toBottomOf="@+id/btnAlarma" />
</androidx.constraintlayout.widget.ConstraintLayout>